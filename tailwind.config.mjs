

/** @type {import("tailwindcss").Config} */
const config = {
    content: [
        "*.html",
        "src/**.mjs",
        "src/**.js",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};


export default config;
